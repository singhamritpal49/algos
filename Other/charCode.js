function values(string) {
    // a = 1, b = 2, c = 3, .... z = 26.
    let count = 0
    for(let i = 0; i < string.length; i++) {
    let value = string[i].charCodeAt() - 96
    count += value
}
return value 
}